spring:
  datasource:
    url: jdbc:mysql://localhost:3306/car_rental_db
    username: root
    password: "81805500"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080